############################################################
#  	Global properties
############################################################
# "handlers" specifies a comma separated list of log Handler
# classes.  These handlers will be installed during VM startup.
# Note that these classes must be on the system classpath.
# By default we only configure a ConsoleHandler, which will only
# show messages at the INFO and above levels.
handlers= java.util.logging.ConsoleHandler

# Default global logging level.
# This specifies which kinds of events are logged across
# all loggers.  For any given facility this global level
# can be overriden by a facility specific level
# Note that the ConsoleHandler also has a separate level
# setting to limit messages printed to the console.
.level= FINE

############################################################
# Handler specific properties.
# Describes specific configuration info for Handlers.
############################################################
# Possible levels are:
#
# SEVERE (highest value)
# WARNING
# INFO
# CONFIG
# FINE
# FINER
# FINEST (lowest value)
#
# But the MD Java API only uses levels from FINE to SEVERE.
# INFO should be the normal setting. FINE should be set for debugging.
java.util.logging.ConsoleHandler.level = FINE

# The following setting specify how logging messages are formatted.
# The SimpleFormatter is part of the JDK and formats messages with a lot
# of context information (time, class, method, level and message).
java.util.logging.ConsoleHandler.formatter = it.grid.storm.namespace.admin.client.BasicFormatter
#java.util.logging.ConsoleHandler.formatter = java.util.logging.SimpleFormatter
